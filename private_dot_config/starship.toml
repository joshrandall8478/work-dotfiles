
format = """
[╭─](white)$username$hostname$shell$conda$container$docker_context$directory$git_branch$git_state$git_status$git_commit$git_metrics$c$cpp$cmake$nodejs$bun$python$java$golang$ruby$rust$rlang$package$cmd_duration
[╰─](white)$character"""


# Get editor completions based on the config schema
"$schema" = 'https://starship.rs/config-schema.json'

# Inserts a blank line between shell prompts
add_newline = true

# Replace the '❯' symbol in the prompt with '➜'
[character] # The name of the module we are configuring is 'character'
success_symbol = '[\$](bold green)' # The 'success_symbol' segment is being set to '➜' with the color 'bold green'
error_symbol = '[\$](bold red)'

# Disable the package module, hiding it from the prompt completely
[package]
disabled = false
symbol = "📦 "
format = "[$symbol$version ]($style)"

[directory]
format = '[$path]($style)[$read_only]($read_only_style) '

[nodejs]
format = '[$symbol($version )]($style)'

[bun]
format = '[$symbol($version )]($style)'

[python]
symbol = '🐍 '
style = "blue bold"
format = '[${symbol}${pyenv_prefix}(${version} ) (\($virtualenv\) )]($style)'

[golang]
format = '[$symbol($version )]($style)'

[ruby]
format = '[$symbol($version )]($style)'

[rust]
format = '[$symbol($version )]($style)'

[rlang]
format = '[$symbol($version )]($style)'

[java]
format = '[${symbol}(${version} )]($style)'

[docker_context]
format = '[🐋 $context ](blue bold)'

[conda]
format = '[$symbol$environment]($style) '

[username]
style_user = 'green bold'
style_root = 'red bold'
show_always = true
format = '[$user]($style)@'

[hostname]
ssh_only = false
format = '[$hostname]($style) '

[sudo]
disabled=false

[os]
disabled=false

[os.symbols]
Gentoo = "󰣨 "
Arch = "󰣇 "

[git_branch]
style="yellow bold"

[git_metrics]
disabled=false
added_style = 'bold blue'
deleted_style = 'bold red'
format = '([+$added]($added_style) )([-$deleted]($deleted_style) )' 

[shell]
disabled=false
nu_indicator="󰟆 nu"
zsh_indicator=" zsh"
bash_indicator=" bash"
fish_indicator="󰈺 fish"
unknown_indicator=" wtf?"
powershell_indicator="󰨊 pwsh"
cmd_indicator=" cmd"
style="purple bold"
format = "[$indicator]($style) "

[cmd_duration]
min_time = 10_000  # Show command duration over 10,000 milliseconds (=10 sec)
format = "took [$duration]($style) "

